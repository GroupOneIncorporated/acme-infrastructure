apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-platform.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
