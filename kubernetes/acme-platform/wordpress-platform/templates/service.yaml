apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress-platform.fullname" . }}
  labels:
    {{- include "wordpress-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordpress-platform.selectorLabels" . | nindent 4 }}
