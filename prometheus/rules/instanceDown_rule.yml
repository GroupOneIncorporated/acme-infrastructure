groups:
  - name: instanceDown_rule
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 30s
        labels:
          serverity: critical problem
        annotations:
          summary: "Instance down"